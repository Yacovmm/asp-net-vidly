@model Vidly.Models.Customer

@{
    ViewBag.Title = Model.Name;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>

<ul>
    <li>MemberShip:  @Model.MemberShipType.RentType</li>
    
    @if (Model.BirthDate.HasValue)
    {
        <li>@Model.BirthDate.Value</li>
    }
</ul>
